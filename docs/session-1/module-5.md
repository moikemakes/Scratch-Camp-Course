# Module 5: Catch Detection

### What You Will Learn
- How to know if your player catches the object

### Step-by-Step Instructions
1. Go to the code for your falling object.
2. Add another `if <> then` block inside the `forever` loop.
3. Put a `touching [Player Sprite Name]?` block (Sensing) inside the new `if`.
4. If it touches the player, it should teleport back up! Use the same block as before: `go to x: [pick random -200 to 200] y: 150`.

### Add Image Here
[PLACE IMAGE HERE – show the blocks that check if the falling object is touching the player, and then resets its position]

### Try It Yourself
Play your game! Try to catch the object. Does it disappear and go back to the top when you touch it?

### Challenge Yourself
Can you add a fun sound effect when the player catches the object? Look in the **Sound** tab!

### Checkpoint
- [ ] The object resets to the top when my player touches it.
